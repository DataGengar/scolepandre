# archives/ — les versions précédentes

Gardées pour référence, plus maintenues, plus jouables depuis le dépôt courant.

| fichier | ce que c'est |
|---|---|
| `scoleopandre.html` | la v1 monofichier |
| `scoleopandre2-monofichier.html` | la v2 monofichier, 2 846 lignes — le point de départ de la v3 |
| `test_level.obj` | maillage d'essai pour le pipeline OBJ de `outils/` |

La v3 est le contenu de `src/`, servi par `index.html`.
